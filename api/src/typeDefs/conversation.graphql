type Channel {
  name: String
  members: [UserReturn]
}
type ConversationUserReturn {
  id: ID
  username: Username
  fullname: String
}
type ConversationReturn {
  id: ID
  channels: [Channel]
  conversationId: Username! # public id, like username
  members: [ConversationUserReturn]
  name: Fullname
  publicKey: String!
}

"""

"""
enum ConversationActivityType {
  JOIN
  LEAVE
}

input ConversationActivityInput {
  conversationId: Username!
}

input ConversationMutationInput {
  conversationId: Username!
  name: Fullname
  description: Fullname
  # channels: [Channel]
}

type Mutation {
  conversationActivity(
    input: ConversationActivityInput!
    type: ConversationActivityType!
  ): ConversationReturn!
  conversationMutation(
    input: ConversationMutationInput!
    type: MutationType!
  ): ConversationReturn!
}
